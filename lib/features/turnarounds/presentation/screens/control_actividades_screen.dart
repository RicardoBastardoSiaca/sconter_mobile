import 'package:flutter/material.dart';

class ControlActividadesScreen extends StatelessWidget {
  const ControlActividadesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Control Actividades')),
      body: const Center(child: Text('Control Actividades')),
    );
  }
}
